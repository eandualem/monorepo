{
  "name": "monorepo",
  "private": true,
  "description": "> TODO: description",
  "author": "eandualem <eandualem@gmail.com>",
  "homepage": "https://github.com/eandualem/monorepo#readme",
  "license": "ISC",
  "scripts": {
    "prepare": "husky install",
    "clean": "lerna run clean --parallel",
    "boot": "yarn clean && lerna clean -y && lerna bootstrap",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "workspaces": [
    "packages/**"
  ],
  "dependencies": {
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eandualem/monorepo.git"
  },
  "bugs": {
    "url": "https://github.com/eandualem/monorepo/issues"
  }
}
