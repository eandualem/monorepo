export const calc = {
  sum: (x: number, y: number): number => x + y,
  sub: (x: number, y: number): number => x - y,
  multiply: (x: number, y: number): number => x * y,
  divide: (x: number, y: number): number => x / y,
  exponent: (x: number, y: number): number => x ** y,
};
